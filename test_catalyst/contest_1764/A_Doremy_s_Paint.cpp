#include <bits/stdc++.h>

#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        for (size_t i = 0; i < n; i++) {
            int a;
            cin >> a;
        }
        cout << 1 << ' ' << n << '\n';
    }
}