#include <bits/stdc++.h>

using namespace std;

using vi = vector<int>;
#define all(x) (x).begin(), (x).end()

using ll = long long;


set<int> merge(const set<int> &l , const set<int> & r) {
    set<int> res;

    for()
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    
}