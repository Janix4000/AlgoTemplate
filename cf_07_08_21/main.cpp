#include <bits/stdc++.h>
using namespace std;

constexpr int N = int(1e6) + 1;

using ll = long long;
using ull = unsigned long long;
template <typename T>
using vec = vector<T>;
using veci = vec<int>;

int main(void)
{
    int qs = 1;
    // cin >> qs;
    while (qs--)
    {
        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
        }
    }
    return 0;
}